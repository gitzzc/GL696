#ifndef __ADC_H__
#define __ADC_H__

#define ADC_BITS	12
#define ADC_VREF	2500

#define ADC_CHANNEL			9
#define ADC_SAMPLE_TIMES	32

extern volatile uint16_t ADCvalue[8];
extern volatile uint16_t ADC_ConvertedValueTab[ADC_SAMPLE_TIMES][ADC_CHANNEL];

void ADC_Config(void);
uint16_t ADC_Get(uint32_t ch);
uint16_t ADC_GetAverage(uint32_t ch);


#endif //__ADC_H__
